# Contacts API

A small API for managing contacts.

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
```

## Running the app

```bash
python app.py
```

## Endpoints

- `GET /health`
- `GET /contacts`
- `GET /contacts/{id}`
- `POST /contacts`